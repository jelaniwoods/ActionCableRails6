<div class="row mb-3">
  <div class="col-md-12">
    <h1 class="display-4">
      Messages

      <%= link_to new_message_path, class: "btn btn-lg btn-outline-secondary" do %>
        <i class="fas fa-plus fa-fw"></i>
      <% end %>
    </h1>
  </div>
</div>

<!-- Table example -->

<div class="row">
  <div class="col-md-12">
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>
              Body
            </th>

            <th>
              Enrollment
            </th>

            <th>
              Resource
            </th>

            <th>
              Actions
            </th>
          </tr>
        </thead>

        <tbody>
          <%= render collection: @messages, partial: "table_row", as: :message %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Card deck example -->

<% @messages.each_slice(3) do |slice| %>
<div class="row">
  <div class="col-md-12">
    <div class="card-deck">
      <%= render collection: slice, partial: "card", as: :message %>
    </div>
  </div>
</div>
<% end %>

<!-- List group example -->

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h1 class="card-header h4 d-flex align-items-center justify-content-between">
        Messages

        <%= link_to new_message_path, class: "btn btn-sm btn-outline-secondary" do %>
          <i class="fas fa-plus fa-fw"></i>
        <% end %>
      </h1>

      <div class="list-group list-group-flush">
        <%= render collection: @messages, partial: "list_item", as: :message %>
      </div>
    </div>
  </div>
</div>
